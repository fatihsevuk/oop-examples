package com.fsoftdev.oop.functionalinterfaces;

public interface MyFunctionalInterface {
     void execute();
     // basically if there are ojnly one abstract meethod in interface , we call that functional interface
    // But it can contain default and static method with their implementation
}
